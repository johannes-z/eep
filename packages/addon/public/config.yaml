---
version: '1.3.3'
slug: 'node_server'
name: 'EnOcean Binding'
description: 'Starts a Bun webserver, exposing a Web API to control EnOcean WRLs'
init: false
arch:
  - aarch64
  - amd64
startup: 'services'
boot: 'auto'
uart: true
services:
  - mqtt:need

ports:
  3000/tcp: 3000
ports_description:
  3000/tcp: 'Webservice API'

options:
  adapter: ''
  adapter_type: 'none'
  baud_rate: 57600
  disable_led: false
  rtscts: false
schema:
  adapter: 'str'
  adapter_type: 'list(none|serial|tcp)'
  baud_rate: 'int'
  disable_led: 'bool'
  rtscts: 'bool'
