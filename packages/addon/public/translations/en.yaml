---
configuration:
  transport_type:
    name: Transport type
    description: The transport type used for the configured connection.
  transport_path:
    name: Transport path
    description: The serial device path or TCP endpoint.
  baud_rate:
    name: Baud rate
    description: Serial transport baud rate.
  rtscts:
    name: RTS/CTS
    description: Enable hardware flow control for serial transport.
